{
  "name": "careerbridge-server",
  "version": "0.0.0",
  "private": true,
  "description": "CareerBridge server (Express 5, Knex, PostgreSQL)",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "^30.5.1"
  }
}
