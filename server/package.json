{
  "name": "careerbridge-server",
  "version": "0.0.0",
  "private": true,
  "description": "CareerBridge server business layer (stored enums, the posting and application state machines, and the S9 error vocabulary)",
  "license": "UNLICENSED",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "^30.5.1"
  }
}
