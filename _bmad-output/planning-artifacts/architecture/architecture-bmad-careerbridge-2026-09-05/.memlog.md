---
scope: CareerBridge whole system (15 use cases UC-A1..UC-M4, FR-X, NFR-1..11) from finalized PRD 2026-09-05
purpose: build-substrate for a 5-student team + Iteration 1 written rationale section for Dr. Ren (stack departure from Maven/JUnit)
altitude: feature (keeps use-case-level stories coherent)
updated: 2026-09-05T21:12
---

- (event) Create intent; coaching path chosen by Oleg (one decision at a time, expects to disagree); no web research, no external handoffs, local files only
- (event) Inputs read: prd.md, addendum.md, PRD .memlog.md, brief addendum, inputs/DECISIONS.md, TEAM.md, group-project-overview.md, problem-statement.md
- (direction) Spine rules per Oleg: only decisions that would conflict if two members decided independently; stable IDs ARCH-nn (not AD-n) so stories can cite them; under two pages plus a rationale section
- (constraint) [ADOPTED D-004] JavaScript end to end: Node.js backend, React frontend, deliberately not TypeScript; Dr. Ren approved verbally, written rationale due Iteration 1 as a section of this document
- (constraint) [ADOPTED course] Web application with multi-layered architecture: presentation, business logic, persistence, data; layer separation and traceability (FR ID -> design -> code -> test) are graded (NFR-10, NFR-11, SC-3)
- (constraint) [ADOPTED] Jest for tests, GitHub Actions on every push (NFR-10/A-30), GitHub Issues for tracking, repo github.com/berdyshevol/careerbridge-csi5324
- (constraint) [ADOPTED brief] Organization membership lives in its own table, not a column on the account, so the parked many-to-many (Q-002) needs no reinterpreting migration
- (constraint) [ADOPTED PRD §3] State machines fixed: Posting Draft->Pending Approval->Live->Filled|Expired|Closed (Pending->Rejected, Expired->Filled); Application Applied->Screening->Interview->Offer->Hired, Applied/Screening/Interview->Rejected|Withdrawn, Offer->Declined
- (question) Open from Oleg: database (PostgreSQL lean, not ratified); backend framework + layout for 5 parallel devs; React router/data-fetching/state lib; auth + RBAC with per-org privacy (NFR-1, NFR-3); where state machines live and how business layer enforces (NFR-4); deployment GCP vs Render/Railway; ownership of Applications, Notifications, audit records (FR-X-4)
- (decision) ARCH-01 PostgreSQL ratified by Oleg (never voted before; Mongo floated once, answered by brief). Binds: data layer, all repositories. Prevents: second engine/dialect, rules re-implemented in app code. Rationale for Iteration 1 section: (1) atomic cascade FR-R6-2/R6-3 under NFR-4 needs a transaction; (2) A-18 one Offer per Posting and FR-A3-3 one active Application per Applicant+Posting are partial unique indexes; (3) FR-X-4 insert-only audit table + FKs give NFR-5 'no orphan'. Alternatives weighed: SQLite (loses persistence on free hosts, dialect drift), MySQL (no partial unique indexes), MongoDB (rejected in brief)
- (direction) Oleg: remaining decisions two at a time in order (2) backend framework+layout (3) frontend (4) auth/RBAC/privacy (5) state machines (6) deployment (7) data ownership; team meeting Monday 2026-09-07 is the deadline
- (question) Oleg proposes Neon (hosted serverless Postgres, per-branch DBs) for dev+prod with docker-compose as offline fallback and for CI; asked me to weigh vs local docker-compose + GitHub Actions service container
- (decision) ARCH-02 One Postgres, three places: local Docker Compose is the developer and CI database (service container), Neon hosts production, same Postgres major pinned everywhere, DATABASE_URL is the only per-environment difference, tests run only against a disposable database; personal Neon branch is the sanctioned fallback for a laptop without Docker. Prevents: dialect drift, shared mutable test DB, slow suite nobody runs. Oleg's deciding reason: suite speed (graded suite must stay fast)
- (decision) ARCH-03 Express 5 + Knex over pg. Rejected: NestJS-without-TS (ceremony without payoff, reverses D-004), Fastify (small ecosystem for students), Prisma (makes graded persistence boundary invisible; partial indexes need raw SQL anyway), raw pg (five SQL styles). Checkable rule: only files under server/src/persistence/ import knex
- (decision) ARCH-04 Layer-first folders (presentation, business, persistence, data) with one business module per use case (business/<area>/<useCase>.js) owned by the use-case owner; repositories per entity, shared, no business rules; routers thin; migrations timestamp-named never renumbered. FR IDs appear in test names in the use-case module's test file (NFR-10). Dependency direction presentation->business->persistence->data only, no skipping, business never sees req/res
- (decision) ARCH-05 One repository (careerbridge-csi5324) with npm workspaces server/ and client/; CI runs one npm test at root. Rejected: two repos (graded commit history and traceability split)
- (decision) ARCH-06 Frontend: Vite + React + React Router (library mode) + TanStack Query + one client/src/api.js wrapper (base URL, credentials, error envelope -> thrown ApiError); auth in one useAuth context; form state local. Greppable rules: components never call fetch; no global state library (Redux/Zustand/etc). Rejected: Next.js (server-side presentation muddies layers), TanStack Router (TS-first), hand-rolled useEffect (five loading/error conventions, stale lists)
- (decision) ARCH-07 Component library is a UX decision (bmad-ux runs next); spine records only the constraint: accessible form controls out of the box (labels, keyboard; NFR-9) and usable at 375 px (NFR-8). MUI is the default if the UX document does not choose otherwise. Oleg pushback: library choice belongs to the Design Engineer
- (direction) Oleg: decisions 4-7 in one message, then finalize with the stack rationale section
- (decision) ARCH-08 Session: server-side session stored in Postgres, HTTP-only SameSite cookie, rolling 8h idle expiry (A-26), bcrypt passwords (NFR-2); session load reads the Account each request so suspension (FR-M3-2) and role change (FR-M3-4) take effect on the next request. Decided against: JWT in localStorage (statelessness unusable because of FR-M3-2 and sliding timeout; XSS surface). Pre-empt per Oleg: someone will propose JWT because the tutorial did
- (decision) ARCH-09 Single origin: API under /api; Vite proxies /api in dev; Express serves client/dist in prod. Prevents: CORS + cross-site cookie debugging, two deployables
- (decision) ARCH-10 Authorization layering: role check is presentation middleware; Organization scope and record ownership are business rules. Checkable rules: (1) every use-case module takes actor {accountId, role, organizationId, recruiterStatus} as its first argument and refuses out-of-scope work itself; (2) recruiter-facing repository queries take organizationId as a mandatory parameter placed in the WHERE clause, never filtered after. These two rules are how FR-R3-3 is proven in the demo
- (decision) ARCH-11 Refusal shape: another Organization's record returns 403 per PRD FR-R3-3 (not 404); ids are UUIDs so enumeration reveals nothing. Oleg: do not reopen a PRD decision from the architecture; flip to 404 later if Dr. Ren prefers hiding existence
- (decision) ARCH-12 State machines: two pure modules in business layer (postingStatus.js, applicationStage.js) exporting the allowed-transition map and assertTransition(from,to) throwing InvalidTransitionError with a human-readable message; no I/O; exhaustive unit test of every edge is the NFR-4 evidence. Database enforces valid values only (CHECK), never transitions. Rejected: scattered ifs, DB triggers for transitions
- (decision) ARCH-13 Every state change commits in one transaction: row update guarded by WHERE status = expected-from, audit row insert, and any Notification the change creates. Persistence exposes withTransaction(fn); business decides the boundary and passes the handle opaquely (still never imports knex). Cascade (accept offer: Application->Hired, Posting->Filled, others->Rejected 'Position filled', notifications) is one use-case module, one transaction
- (decision) ARCH-14 No in-process event bus / EventEmitter for domain events; Notifications are written by the use-case module inside its transaction by calling the notification repository. Pre-empt per Oleg: someone will propose an emitter for notifications by week six; reason: outside the transaction and outside traceability
- (decision) ARCH-15 Expiry (FR-M2-4) is derived on read, no scheduler: stored status stays live; one predicate in the posting repository (isEffectivelyLive: status='live' AND expires_at > now()) is the only definition of Live for Job List, apply checks, oversight counts; the state module treats Expired as computed input for Expired->Filled. Oleg: make the single predicate an explicit rule (week-seven bug: a list that forgot it)
- (decision) ARCH-16 Deployment: one Docker image (Express serving built client), configured only by environment variables, no local disk. Host: Render free web service + Neon Postgres; Cloud Run + Cloud SQL is the named alternative if Dr. Ren requires GCP; rationale section asks for Render's approval explicitly ('another approved environment'). CI: GitHub Actions on every push runs lint + both Jest suites against a Postgres service container; Render auto-deploys main on green. Warm the service before a presentation (NFR-7)
- (decision) ARCH-17 Resume storage: bytea in a separate resume_files table, immutable rows (new upload = new row, profile points at current; Applications keep their version = FR-A3-5 snapshot). Oleg pushback accepted: Neon free tier ~0.5 GB, so cap lowered to 2 MB (PRD A-2 said 5 MB; addendum note to adjust, Oleg confirms with team) and storage limit recorded as a named risk with object storage as the escape hatch. Reason for bytea: one connection string matters more to a student team than elegance
- (decision) ARCH-18 Data ownership. Application: parents Applicant Account + Posting (both NOT NULL); created only by the apply use case, mutated only through the transition modules; Stage history is read from audit events, not stored twice (conscious trade-off per Oleg: couples applicant-facing history to audit format, accepted for the semester); rejection reason lives only on the audit event; interviews are their own table owned by the Application. Notification: owned by recipient Account, written only inside a use-case transaction, recipient may flip read flag only, carries entity_type+entity_id, never deleted. Audit events: one table (entity_type, entity_id, field, old, new, actor, reason, at), insert-only enforced by a DB trigger raising on UPDATE/DELETE; cascade rows record the triggering actor. Settings table for Application Cap; reference data tables with active flag; stage label table; all changes audited. Resume owned by Applicant (see ARCH-17)
- (assumption) Versions to verify at scaffold time (no web research this run): Express 5.x, Knex 3.x, pg 8.x, React 19, React Router 7 (library mode), TanStack Query 5, Vite 6/7, Node 22 LTS, Postgres 17 (match Neon's default major), MUI 6/7, Jest 30
- (event) All seven open decisions closed by Oleg; finalize begins (distill, reconcile, reviewer gate, rationale section)
- (event) Finalize step 1: spine distilled from memlog (ARCH-01..18, conventions, stack, seed diagrams, capability map, deferred, open questions, rationale section)
- (event) Finalize step 2 reconcile: 3 subagents (PRD, PRD+brief addenda, course overview + Oleg's rules) -> reviews/reconcile-prd.md (3 high), reconcile-addenda.md (4 high), reconcile-course.md (1 critical length ~5 pages, 3 high missing decisions)
- (event) Finalize step 3 reviewer gate: lint clean; rubric walker (4 high, 13 medium), reality-check lens (3 high: Jest-on-Vite, Express 5 splat fallback, trust proxy for Secure cookie), adversarial lens (2 critical: stored-vs-effective guard on Expired Posting, audit row vocabulary; 8 high) -> reviews/review-*.md
- (decision) Length resolution: spine body cut to a terse contract; shared vocabularies moved to binding companion SHAPES.md (S1..S11: enums, status tables, indexes, effective_status fragment, audit row, notification row, req.actor, signatures, errors, paths/envelopes, env vars); map + diagrams + tree + seed cast moved to CAPABILITY-MAP.md. ARCH rules bind the companions by reference
- (decision) ARCH-19 audit_events: exact row (S5), auditRepository.record sole writer, ORDER BY created_at, seq; every reason only on audit; non-admin history shows actor role only (privacy of cascade actor)
- (decision) ARCH-20 migrations and seeds: Librarian-owned initial migration set of the whole ERD before use-case branches; add-only afterwards, one per PR, shared-table columns via Librarian; seeds/base (Administrator from env, reference data) everywhere; seeds/demo (PRD §2 cast incl. Pending Bear Staffing for UC-M1) only with SEED_DEMO; tests use factories + truncate per file; CI runs migrate up/down/up
- (decision) ARCH-21 repository workflow: GitHub Issues one per use case/NFR; branch <issue>-<slug>; PR into protected main with CI green + one non-author reviewer; merge commits never squash (graded per-member history); merge is deploy
- (decision) ARCH-22 Jest everywhere: server CommonJS zero-config --runInBand + supertest; client Jest via babel-jest + jsdom + RTL; only client/src/config.js touches import.meta.env. Rejected: Vitest (Jest is fixed by team)
- (change) Amended in place: ARCH-01 exact index predicates (non-withdrawn, not active); ARCH-03 grep fixed (migrations exempt) + Express 5 async throw; ARCH-04 signature, no cross-imports; ARCH-06 unread count from Iteration 1; ARCH-08 SameSite=Lax/Secure/trust proxy, pool from persistence/db.js, CSRF stance, login rate limit; ARCH-09 /{*splat} fallback, /api 404; ARCH-10 req.actor + two guards; ARCH-11 404 for non-Live to visitors; ARCH-12 assertPostingAllows for FR-R4-4; ARCH-13 effective-vs-stored guard, ConcurrentChangeError, creation audit row, cap FOR UPDATE + settings in-transaction; ARCH-15 S4 fragment, DB clock, end-of-day TZ; ARCH-16 migrate at start, deploy after CI, health, pino, pg_dump; ARCH-17 multer + magic bytes + getResume scope; ARCH-18 membership table + statuses, stage_changed_at cache, notification shape, delete rule
- (change) PRD reconcile items applied: FR-A2-2 (ARCH-11), FR-R4-4 (ARCH-12/15), NFR-6 (S3 indexes + S10 pagination), FR-R2-2 delete (ARCH-18), recipient rule for 'notifies the Recruiter' (S6), FR-A2-1 sort column approved_at (S2/S3), admin seed creds (S11), rationale FR-A3-3 wording fixed to non-withdrawn. Not applied (story-level): FR-M3-5 self-suspend, FR-M4-1 lowering cap below count, role-change side effects, approving an already-expired Posting -> listed under Deferred
- (change) Stack versions per reality lens: Node 'current LTS (22 or 24)', Vite 7, Postgres 'Neon default (17 or 18)', test deps added; rationale gains course-default -> choice -> status table and 'tracking' (GitHub Issues) sentence
- (event) PRD addendum A-2 row annotated with the 2 MB proposal (ARCH-17) for team confirmation
- (event) Second-pass recheck (reviews/review-recheck.md): 31 of 32 first-pass critical/high closed, 1 partial (demo seed on deployed demo), 0 open; new issues fixed: ARCH-10(3) list-vs-single-record scoping so 403 is producible; interview entity in audit CHECK; organization_members gets id PK; enums export stored vs effective sets; AccountSuspendedError; SEED_DEMO on deployed demo; getMyResume/getSubmittedResume as two scoped modules; permitted caches enumerated
- (change) Compaction: mechanics moved out of ARCH-08/13/16/17/20/22 into SHAPES.md S12-S16; Binds/Prevents to one italic line; stack table merged. Body excluding rationale ~2,150 words (frontmatter, 22 rules, conventions, stack, deferred, open questions); rationale ~750 words. Oleg's under-two-pages target not met on raw count; reviewers' required rules kept in preference to cutting binding content — reported to Oleg with cut options
- (event) spine finalized
